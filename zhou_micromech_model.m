check = 0;
% Coarse Aggregate type: 0 - GG, 1 - CS
aggregate_type = [0,1];
% Sieve Size (mm)
sieve_size = [37.5, 25, 19, 12,5, 9.5, 4.75, 2.36];

while check == 0
   % SUMMATION OVER COARSE AGGREGATE SIZE
   for sieve_diam = sieve_size
       
   end
end